import React from 'react';
import UploadForm from './components/UploadForm';

const App: React.FC = () => {
    return (
        <div>
            <h1>Upload Video to YouTube</h1>
            <UploadForm />
        </div>
    );
};

export default App;
